import React from 'react';
import { NavLink } from 'react-router';

const Header = () => {
    return (
        <div>
          <NavLint>Home</NavLink>  
        </div>
    );
};

export default Header;