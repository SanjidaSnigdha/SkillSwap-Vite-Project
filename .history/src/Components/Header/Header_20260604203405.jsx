import React from 'react';
import { NavLink } from 'react-router';

const Header = () => {
    return (
        <div>
          <NavLinto>Home</NavLink>  
        </div>
    );
};

export default Header;